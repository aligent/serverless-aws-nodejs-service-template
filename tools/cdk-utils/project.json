{
    "name": "cdk-utils",
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "tools/cdk-utils/src",
    "projectType": "library",
    "tags": ["cdk", "utils"],
    "// targets": "to see all targets run: nx show project cdk-utils --web",
    "targets": {
        "typecheck": {
            "executor": "nx:run-commands",
            "options": {
                "cwd": "{projectRoot}",
                "color": true,
                "command": "tsc --noEmit --pretty"
            }
        }
    }
}
