version: '3.2'

services:
  offline:
    image: aligent/serverless
    command: serverless offline --stage dev
    volumes: 
      - .:/app
      - ~/.npm:/home/node/.npm
      - ~/.aws:/home/node/.aws
